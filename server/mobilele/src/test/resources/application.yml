spring:
    datasource:
        url: jdbc:h2:mem:mobilele
        driverClassName: org.h2.Driver

    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
    sql:
        init:
            mode: always
            data-locations: data.sql
            schema-locations: data.sql